import 'package:flutter/material.dart';

class ClerkPage extends StatelessWidget {
  const ClerkPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.white,
      child: const Center(child: Text('Clerk Page')),
    );
  }
}
